import { MilliFormatPipe } from './milli-format.pipe';

describe('MilliFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MilliFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
